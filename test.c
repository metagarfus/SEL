#include "t.h"

int ·g() {
  return 0;
}

int main(int argc, char **argv) {
  ·g();
 //make_t(7);

  /* int f = argc >= 3 ? 3 : 0, g = argc > 5 ? atoi(argv[1]) : 5;
  if (f == 3)
    if (g == 3)
      printf("g = 3\n");
    else
      printf("f = 3\n");
  else
  printf("NONE\n");*/
}
